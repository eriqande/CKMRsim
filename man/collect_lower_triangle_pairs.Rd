% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/summarize_missing_data.R
\name{collect_lower_triangle_pairs}
\alias{collect_lower_triangle_pairs}
\title{Collect Lower Triangle Pairs by Value}
\usage{
collect_lower_triangle_pairs(M)
}
\arguments{
\item{M}{A square numeric matrix. Assumed to be symmetric with integer-like values.}
}
\value{
A named list. Each element corresponds to a unique value in the lower triangle of \code{M},
and contains a list with components r and c, the row and column indices (with r > c)
where that value occurs.
}
\description{
Given a symmetric square integer matrix \code{M}, this function extracts all (r, c)
index pairs from the lower triangle (r > c) for each unique value \verb{V*} present
in that region of the matrix. The result is a named list where each name corresponds
to a value \verb{V*}, and each element is a list containing two integer vectors: r and c.
}
\examples{
set.seed(1)
M <- matrix(sample(80:180, size = 100^2, replace = TRUE), nrow = 100)
result <- collect_lower_triangle_pairs(M)
str(result[["100"]])

}
